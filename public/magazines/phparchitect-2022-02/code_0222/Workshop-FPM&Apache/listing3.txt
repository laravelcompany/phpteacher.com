<VirtualHost *:80>
	ServerAdmin webmaster@localhost
	ServerName fresh.test
	ServerAlias www.fresh.test
	DocumentRoot /home/vagrant/fresh/public

	<Directory /home/vagrant/fresh/public>
    AllowOverride All
    Require all granted
	</Directory>

	<FilesMatch ".+\.ph(ar|p|tml)$">
    SetHandler "proxy:unix:/var/run/php/php8.0-fpm.sock|fcgi://localhost"
	</FilesMatch>

	ErrorLog ${APACHE_LOG_DIR}/fresh.test-error.log
	CustomLog ${APACHE_LOG_DIR}/fresh.test-access.log combined
</VirtualHost>